{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspace @mollify/lms"
  },
  "workspaces": [
    "packages/types",
    "packages/cli",
    "packages/tts",
    "packages/molly",
    "packages/lms",
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.6.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}
